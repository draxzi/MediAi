modules = ["nodejs-20"]
run = "npm install && npm run dev -- --host 0.0.0.0 --port 3000"

[deployment]
run = ["sh", "-lc", "npm ci && npm run build && npm run preview -- --host 0.0.0.0 --port 3000"]

[[ports]]
localPort = 3000
externalPort = 80
